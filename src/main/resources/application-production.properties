spring.jpa.open-in-view=false

# Keep serverless instances from holding unnecessary idle Cloud SQL connections.
spring.datasource.hikari.minimum-idle=${DB_POOL_MIN_IDLE:0}
spring.datasource.hikari.maximum-pool-size=${DB_POOL_MAX_SIZE:10}
spring.datasource.hikari.connection-timeout=${DB_POOL_CONNECTION_TIMEOUT_MS:30000}
spring.datasource.hikari.validation-timeout=${DB_POOL_VALIDATION_TIMEOUT_MS:5000}
spring.datasource.hikari.idle-timeout=${DB_POOL_IDLE_TIMEOUT_MS:600000}
spring.datasource.hikari.max-lifetime=${DB_POOL_MAX_LIFETIME_MS:1800000}

# Allow the deployed Vercel client by default; deployment env vars can still override this.
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:https://sopra-fs26-group-38-client.vercel.app}

